<h1> Craig's List on Rails </h1>
<h2>Categories</h2>
<% @categories.each do |category| %>
  <p><%= link_to category.name, category_path(category) %></p>
 <!--  this can also be category -->
<% end %>
